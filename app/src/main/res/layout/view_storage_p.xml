<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorMyBack"
    android:gravity="center"
    android:orientation="vertical">

    <androidx.appcompat.widget.LinearLayoutCompat
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        android:orientation="vertical">

        <Button
            android:id="@+id/button_settings"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="10dp"
            android:background="@color/colorMyButtonSettings"
            android:text="Settings"
            android:textColor="@color/colorMyText"></Button>

        <Button
            android:id="@+id/storage_a"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/colorMyButton"

            android:text="Storage access"
            android:textColor="@color/colorMyText"></Button>
    </androidx.appcompat.widget.LinearLayoutCompat>
</RelativeLayout>